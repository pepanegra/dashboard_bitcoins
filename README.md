# dashboard_bitcoins

para correr el programa instala el entorno virtual 
 python -m venv .venv

luego isntalar las dependencias 
pip install -r requirements.txt

para agregar dependencia utilizar este comando
 pip freeze > requirements.txt

comando para correr fastapi
 uvicorn api:app --reload


Nota: hacer todo esto en la carpeta backend
